#SPDX-License-Identifier: MIT-0
---
# handlers file for roles/docker


- name: start docker
  service:
    name: docker
    state: started
    enabled: "{{ docker_service_enabled }}"
  become: yes

- name: Restart docker
  service:
    name: docker
    state: restarted
    enabled: true
